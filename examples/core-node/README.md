# Core (Node) Example

## Run

```bash
npm install
npm run start
```

## What it demonstrates

- `oinDeep()` deep tree nodes
- `subscribeUpdate()` update recording
- `batch()` coalescing updates
- `mergeUpdates()` + `invertUpdate()` + `replay()` for rollback

