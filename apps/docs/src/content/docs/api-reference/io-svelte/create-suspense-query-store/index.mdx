---
title: "createSuspenseQueryStore"
description: "函数 API 参考。"
---


## 何时使用

当你需要该包提供的这项行为能力时使用此函数。

## 签名

```ts
<TData, TError = Error, TSelected = TData>(options: IoCreateQueryStoreOptions<TData, TError, TSelected>) => IoSuspenseQueryStore<TData, TError, TSelected>
```

## 参数

| Name | Type |
| --- | --- |
| options | `IoCreateQueryStoreOptions<TData, TError, TSelected>` |

## 返回

`IoSuspenseQueryStore<TData, TError, TSelected>`

## 最小示例

```ts
import { createSuspenseQueryStore } from '@iostore/svelte';

const result = createSuspenseQueryStore(/* ...args */);
console.log(result);
```

## 常见误用

- 调用前请确认参数类型与可空性约束。
- 注意导入路径与运行环境（服务端/客户端）是否匹配。

## 相关 API

- [createInfiniteQueryStore](../create-infinite-query-store/)
- [createMutationStore](../create-mutation-store/)
- [createQueryStore](../create-query-store/)
- [createSuspenseInfiniteQueryStore](../create-suspense-infinite-query-store/)

## 源码

- `packages/io-svelte/src/lib/stores.ts`
