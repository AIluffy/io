---
title: "createSuspenseInfiniteQueryStore"
description: "函数 API 参考。"
---


## 何时使用

当你需要该包提供的这项行为能力时使用此函数。

## 签名

```ts
<TData, TError = Error, TPageParam = unknown, TSelected = InfiniteData<TData, TPageParam>>(options: IoCreateInfiniteQueryStoreOptions<TData, TError, TPageParam, TSelected>) => IoSuspenseInfiniteQueryStore<TData, TError, TPageParam, TSelected>
```

## 参数

| Name | Type |
| --- | --- |
| options | `IoCreateInfiniteQueryStoreOptions<TData, TError, TPageParam, TSelected>` |

## 返回

`IoSuspenseInfiniteQueryStore<TData, TError, TPageParam, TSelected>`

## 最小示例

```ts
import { createSuspenseInfiniteQueryStore } from '@iostore/svelte';

const result = createSuspenseInfiniteQueryStore(/* ...args */);
console.log(result);
```

## 常见误用

- 调用前请确认参数类型与可空性约束。
- 注意导入路径与运行环境（服务端/客户端）是否匹配。

## 相关 API

- [createInfiniteQueryStore](../create-infinite-query-store/)
- [createMutationStore](../create-mutation-store/)
- [createQueryStore](../create-query-store/)
- [createSuspenseQueryStore](../create-suspense-query-store/)

## 源码

- `packages/io-svelte/src/lib/stores.ts`
