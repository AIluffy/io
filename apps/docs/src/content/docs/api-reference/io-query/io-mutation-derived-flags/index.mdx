---
title: "IoMutationDerivedFlags"
description: "Mutation UI 状态布尔派生标记类型。"
---

## 何时使用

用于表达 Mutation 状态的布尔标记，便于 UI 条件渲染。

## 签名

```ts
type IoMutationDerivedFlags = {
  readonly isIdle: boolean;
  readonly isPending: boolean;
  readonly isSuccess: boolean;
  readonly isError: boolean;
};
```

## 最小示例

```ts
if (mutation.flags.isPending) {
  showSpinner();
}
```

## 常见误用

- 将各标记视为可同时为真；它们由单一状态派生。

## 相关 API

- [IoMutation](../io-mutation/)
- [deriveMutationFlags](../derive-mutation-flags/)

## 源码

- `packages/io/src/lib/query/types.ts`
