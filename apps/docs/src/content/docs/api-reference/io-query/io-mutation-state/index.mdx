---
title: "IoMutationState"
description: "Mutation 实例的响应式状态结构。"
---

## 何时使用

当你直接读取 mutation 快照时使用该类型。

## 签名

```ts
type IoMutationState<TData = unknown, TError = Error> = {
  readonly status: IoMutationStatus;
  readonly data: TData | undefined;
  readonly error: TError | null;
  readonly variables: unknown | undefined;
  readonly submittedAt: number;
};
```

## 最小示例

```ts
const state = mutation.snapshot();
if (state.status === 'error') {
  console.error(state.error);
}
```

## 常见误用

- 仅凭该状态类型就假设 `variables` 具备具体结构。

## 相关 API

- [IoMutation](../io-mutation/)
- [IoMutationStatus](../io-mutation-status/)

## 源码

- `packages/io/src/lib/query/types.ts`
