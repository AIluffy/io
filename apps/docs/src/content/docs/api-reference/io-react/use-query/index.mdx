---
title: "useQuery"
description: "函数 API 参考。"
---

## 签名

```ts
<TData, TError = Error>(options: UseQueryOptions<TData, TError>) => UseQueryResult<TData, TError>
```

## 参数

| Name | Type |
| --- | --- |
| options | `UseQueryOptions<TData, TError>` |

## 关键说明

- `options.client` 可选，不必传。
- 未传时内部使用 [getDefaultClient](/api-reference/io-query/get-default-client/)。
- 仅在这些场景传自定义 `client`：SSR 每请求隔离、测试隔离、多缓存域。

## 返回

`UseQueryResult<TData, TError>`

## 最小示例

```ts
import { useQuery } from '@iostore/react';

const result = useQuery({
  key: ['todos'],
  queryFn: fetchTodos,
});
```

```ts
import { createQueryClient } from '@iostore/store/query';
import { useQuery } from '@iostore/react';

const client = createQueryClient();
const result = useQuery({
  client,
  key: ['todos'],
  queryFn: fetchTodos,
});
```

## 相关 API

- [useIO](../use-io/)
- [useIOSelector](../use-ioselector/)
- [useMutation](../use-mutation/)
- [useSuspenseQuery](../use-suspense-query/)
- [UseQueryResult](../io-query-result/)

## 源码

- `packages/io-react/src/lib/use-query.ts`
