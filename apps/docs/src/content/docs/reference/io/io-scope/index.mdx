---
title: "IoScope"
description: "type export"
---


## 签名

```ts
IoScope<T>
```

## 属性

| Name | Type |
| --- | --- |
| get | `() => IsArray<T> extends true ? NormalizeIoInput<T> extends readonly (infer U)[] ? (IsArray<U> extends true ? NormalizeIoInput<U> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer...` |
| commit | `(fn: (draft: IsArray<T> extends true ? NormalizeIoInput<T> extends readonly (infer U)[] ? (IsArray<U> extends true ? NormalizeIoInput<U> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly...` |
| snapshot | `() => IsArray<T> extends true ? NormalizeIoInput<T> extends readonly (infer U)[] ? (IsArray<U> extends true ? NormalizeIoInput<U> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer...` |
| subscribe | `(fn: (v: IsArray<T> extends true ? NormalizeIoInput<T> extends readonly (infer U)[] ? (IsArray<U> extends true ? NormalizeIoInput<U> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (infer U)[] ? (IsArray<...> extends true ? NormalizeIoInput<...> extends readonly (in...` |
| subscribeUpdate | `(fn: (u: IoUpdate) => void) => IoUnsubscribe` |

## 错误

- 本库未定义标准化错误码。
- 变更失败可通过 `onError(listener)` 上报（operation、path、value）。

## 源码

- `packages/io/src/lib/utils/types/types.ts`
