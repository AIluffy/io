---
title: "Primitive"
description: "type export"
---


## 签名

```ts
Primitive
```

## 属性

| Name | Type |
| --- | --- |
| toString | `(() => string) \| (() => string) \| ((radix?: number) => string) \| ((radix?: number) => string) \| (() => string)` |
| valueOf | `(() => string) \| (() => number) \| (() => bigint) \| (() => boolean) \| (() => symbol)` |
| toLocaleString | `(() => string) \| { (locales?: string \| string[], options?: Intl.NumberFormatOptions): string; (locales?: Intl.LocalesArgument, options?: Intl.NumberFormatOptions): string; } \| ((locales?: Intl.LocalesArgument, options?: BigIntToLocaleStringOptions) => string)` |

## 错误

- 本库未定义标准化错误码。
- 变更失败可通过 `onError(listener)` 上报（operation、path、value）。

## 源码

- `packages/io/src/lib/utils/types/types.ts`
