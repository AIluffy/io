---
title: 'toWritable'
description: 'function export'
---

## 签名

```ts
<T>(unit: IoUnit<T>, options?: IoSvelteOptions) => Writable<T>;
```

## 参数

| Name    | Type              |
| ------- | ----------------- |
| unit    | `IoUnit<T>`       |
| options | `IoSvelteOptions` |

## 返回

`Writable<T>`

## 错误

- 本库未定义标准化错误码。
- 变更失败可通过 `onError(listener)` 上报（operation、path、value）。

## 源码

- `packages/io-svelte/src/lib/stores.ts`
