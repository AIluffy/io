---
title: Quick Start
description: Install IO and build your first state in minutes.
sidebar:
  order: 2
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

## Install

<Steps>

1. Install the core package:

   ```bash
   npm install io-store
   ```

2. Add an adapter (optional):

   ```bash
   npm install io-react
   ```

</Steps>

## Create state

```ts
import { io } from 'io-store';

const count = io(0);

count(); // read
count(1); // write
count((v) => v + 1); // update
```

## Use in your framework

<Tabs>
  <TabItem label="Core">
    ```ts
    import { io, batch } from 'io-store';

    const store = io({ count: 0, user: { name: 'Ada' } });

    store.count();
    store.user.name('Grace');

    batch(() => {
      store.count((v) => v + 1);
      store.count((v) => v + 1);
    });
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { io } from 'io-store';
    import { useIo } from 'io-react';

    const count = io(0);

    export function Counter() {
      const value = useIo(count);
      return <button onClick={() => count((v) => v + 1)}>{value}</button>;
    }
    ```
  </TabItem>
  <TabItem label="Vue">
    ```ts
    import { io } from 'io-store';
    import { useIo } from 'io-vue';

    const count = io(0);

    export function useCounter() {
      const state = useIo(count);
      const inc = () => count((v) => v + 1);
      return { state, inc };
    }
    ```
  </TabItem>
  <TabItem label="Svelte">
    ```ts
    import { io } from 'io-store';
    import { toWritable } from 'io-svelte';

    const count = io(0);
    export const countStore = toWritable(count);
    ```
  </TabItem>
</Tabs>

## Next steps

- Learn the structure of Units and Scopes in [Core Concepts](/en/core-concepts/units-scopes/).
- Add behavior layers in [Behaviors](/en/behaviors/overview/).
- Read the auto-generated [API Reference](/en/reference/).
