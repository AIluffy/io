---
title: Svelte
description: Use toReadable and toWritable.
sidebar:
  order: 4
---

`io-svelte` provides adapters compatible with Svelte stores.

## toReadable

```ts
import { io } from 'io-store';
import { toReadable } from 'io-svelte';

const store = io({ count: 0 });
const readable = toReadable(store.count);
```

## toWritable

```ts
import { toWritable } from 'io-svelte';

const writable = toWritable(store.count);
```

## Scheduling

```ts
const readable = toReadable(store.count, { schedule: 'sync' });
```

Available: `sync` | `microtask` | `animationFrame`.

## Next Steps

- Learn [Subscriptions](/en/guides/subscriptions/).
- Combine with [Persist](/en/behaviors/persist/).
