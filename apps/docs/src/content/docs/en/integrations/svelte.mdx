---
title: Svelte
description: Use toReadable and toWritable.
sidebar:
  order: 5
---

The npm package `@iostore/svelte` provides `toReadable` / `toWritable` adapters compatible with Svelte stores.

## toReadable

```ts
import { io } from '@iostore/store';
import { toReadable } from '@iostore/svelte';

const store = io({ count: 0 });
const readable = toReadable(store.count);
```

## toWritable

```ts
import { toWritable } from '@iostore/svelte';

const writable = toWritable(store.count);
```

## Scheduling

```ts
const readable = toReadable(store.count, { schedule: 'sync' });
```

Available: `sync` | `microtask` | `animationFrame`.

## Next Steps

- Learn [Subscriptions](/en/guides/subscriptions/).
- Combine with [Persist](/en/behaviors/persist/).
- For SSR, see the [SvelteKit](/en/integrations/sveltekit/) practical guide.
