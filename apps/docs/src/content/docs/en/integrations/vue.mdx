---
title: Vue
description: Vue Composition API helpers for IO.
sidebar:
  order: 3
---

## useIO

```ts
import { io } from 'io-store';
import { useIO } from 'io-vue';

const count = io(0);

export function useCounter() {
  const state = useIO(count);
  const inc = () => count((v) => v + 1);
  return { state, inc };
}
```

## ioRef

`ioRef` adapts a Unit into a Vue `ref`.

```ts
import { io } from 'io-store';
import { ioRef } from 'io-vue';

const count = io(0);
const countRef = ioRef(count);
```

## Next steps

- Learn how to subscribe in [Vanilla JS](/en/integrations/vanilla/).
- Add tooling in [Behaviors: DevTools](/en/behaviors/devtools/).
