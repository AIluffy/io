---
title: Vue
description: Vue Composition API helpers for OIN.
sidebar:
  order: 3
---

## useOin

```ts
import { oin } from '@oin/store';
import { useOin } from '@oin/vue';

const count = oin(0);

export function useCounter() {
  const state = useOin(count);
  const inc = () => count((v) => v + 1);
  return { state, inc };
}
```

## oinRef

`oinRef` adapts a Unit into a Vue `ref`.

```ts
import { oin } from '@oin/store';
import { oinRef } from '@oin/vue';

const count = oin(0);
const countRef = oinRef(count);
```

## Next steps

- Learn how to subscribe in [Vanilla JS](/en/integrations/vanilla/).
- Add tooling in [Behaviors: DevTools](/en/behaviors/devtools/).
