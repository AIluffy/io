---
title: Lynx
description: Connect IO with useIO in ReactLynx.
sidebar:
  order: 3
---

The npm package `@iostore/lynx` provides `useIO` and `useIOSelector` for subscribing to IO sources in ReactLynx.

## Basic Usage

```tsx
import { root } from '@lynx-js/react';
import { io } from '@iostore/store';
import { useIO } from '@iostore/lynx';

const count = io(0);

function Counter() {
  const value = useIO(count);
  return (
    <view>
      <text>{value}</text>
      <view bindtap={() => count.set((v) => v + 1)}>
        <text>+1</text>
      </view>
    </view>
  );
}

root.render(<Counter />);
```

## TypeScript Setup

```json
{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@lynx-js/react"
  }
}
```

## useIOSelector

```tsx
import { useIOSelector } from '@iostore/lynx';

const store = io({ count: 0, other: 0 });

function CountOnly() {
  const count = useIOSelector(store, (state) => state.count);
  return <text>{count}</text>;
}
```

With custom equality:

```tsx
const parity = useIOSelector(store, (state) => ({ p: state.count % 2 }), {
  isEqual: (prev, next) => prev.p === next.p,
});
```

## Scheduling

```tsx
const value = useIO(count, { schedule: 'microtask' });
```

Available: `sync` | `microtask` | `animationFrame`.

## Query / Resource

Install query package:

```bash
npm i @iostore/query
```

`@iostore/lynx` exposes `useQuery` and `useResource` backed by `@iostore/query`:

```tsx
import { useQuery } from '@iostore/lynx';

function UserName(props: { id: number }) {
  const user = useQuery({
    key: ['user', props.id],
    queryFn: async () => {
      'background only';
      return fetch(`/api/users/${props.id}`).then((r) => r.json());
    },
  });
  return <text>{user.data?.name ?? 'loading'}</text>;
}
```

Note: in Lynx, network side effects should run on the background thread. Mark `queryFn` with `'background only'` when needed.

## Notes

- Use Lynx intrinsic elements and events: `view` / `text` / `input` with `bindtap` / `bindinput`.
- Use `root.render(...)` as the app entry instead of web `createRoot`.

## Next Steps

- Combine with [Persist](/en/behaviors/persist/).
- Read [Lynx Quick Start](https://lynxjs.org/guide/start/quick-start.html).
