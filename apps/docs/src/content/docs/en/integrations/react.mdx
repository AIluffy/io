---
title: React
description: Use IO with React via the `useIo` hook.
sidebar:
  order: 2
---

## Hook usage

```tsx
import { io } from 'io-store';
import { useIo } from 'io-react';

const count = io(0);

export function Counter() {
  const value = useIo(count);
  return <button onClick={() => count((v) => v + 1)}>{value}</button>;
}
```

## Derived in React

```tsx
import { derived, io } from 'io-store';
import { useIo } from 'io-react';

const store = io({ count: 0 });
const doubled = derived([store.count], (value) => value * 2);

export function Summary() {
  const value = useIo(doubled);
  return <span>{value}</span>;
}
```

## Next steps

- Add persistence in [Behaviors: Persist](/en/behaviors/persist/).
- See advanced typings in [TypeScript](/en/guides/typescript/).
