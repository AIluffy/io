---
title: "useQuery"
description: "Function API reference."
---


## When to Use

Use this function when you need this behavior from the package API.

## Signature

```ts
<TData, TError = Error, TSelected = TData>(options: IoUseQueryOptions<TData, TError, TSelected>) => IoVueQueryResult<TData, TError, TSelected>
```

## Parameters

| Name | Type |
| --- | --- |
| options | `IoUseQueryOptions<TData, TError, TSelected>` |

## Returns

`IoVueQueryResult<TData, TError, TSelected>`

## Minimal Example

```ts
import { useQuery } from '@iostore/vue';

const result = useQuery(/* ...args */);
console.log(result);
```

## Common Pitfalls

- Validate argument types and nullability before calling.
- Confirm import path and runtime environment (server/client) expectations.

## Related APIs

- [ioRef](../io-ref/)
- [useInfiniteQuery](../use-infinite-query/)
- [useIO](../use-io/)
- [useIOSelector](../use-ioselector/)

## Source

- `packages/io-vue/src/lib/query.ts`
