---
title: "IoDevtools"
description: "type export"
---


## Signature

```ts
IoDevtools
```

## Properties

| Name | Type |
| --- | --- |
| getState | `() => IoDevtoolsState` |
| subscribe | `(listener: (event: IoDevtoolsEvent) => void) => Unsubscribe` |
| setEnabled | `(enabled: boolean) => void` |
| pause | `() => void` |
| resume | `() => void` |
| destroy | `() => void` |
| clear | `() => void` |
| timeTravel | `{ undo: () => boolean; redo: () => boolean; goTo: (index: number) => boolean; }` |
| export | `{ json: () => string; reduxDevToolsImport: () => ReduxDevToolsImportState; }` |
| connectReduxDevToolsExtension | `(options?: { window?: unknown; name?: string; }) => IoDevtoolsBridge \| null` |

## Errors

- This library does not define standardized error codes.
- Mutations can report failures through `onError(listener)` (operation, path, value).

## Source

- `packages/io-devtools/src/lib/types.ts`
