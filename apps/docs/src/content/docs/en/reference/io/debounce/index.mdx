---
title: "debounce"
description: "Function API reference."
---


## When to Use

Use this function when you need this behavior from the package API.

## Signature

```ts
<T>(waitMs: number, options?: DebounceBehaviorOptions) => IoBehavior<T>
```

## Parameters

| Name | Type |
| --- | --- |
| waitMs | `number` |
| options | `DebounceBehaviorOptions` |

## Returns

`IoBehavior<T>`

## Minimal Example

```ts
import { debounce } from '@iostore/store/behavior';

const result = debounce(/* ...args */);
console.log(result);
```

## Common Pitfalls

- Validate argument types and nullability before calling.
- Confirm import path and runtime environment (server/client) expectations.

## Related APIs

- [applyUpdate](../apply-update/)
- [batch](../batch/)
- [createAbortError](../create-abort-error/)
- [createHistory](../create-history/)

## Source

- `packages/io/src/lib/extensions/behaviors/debounce.ts`
