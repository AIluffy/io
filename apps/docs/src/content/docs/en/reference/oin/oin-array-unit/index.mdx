---
title: "OinArrayUnit"
description: "type export"
---


## Signature

```ts
OinArrayUnit<T>
```

## Properties

| Name | Type |
| --- | --- |
| push | `(...items: T[]) => void` |
| pop | `() => T \| undefined` |
| splice | `(start: number, deleteCount: number, ...items: T[]) => void` |
| sort | `(compareFn?: (a: T, b: T) => number) => void` |
| commit | `(fn: (draft: T[]) => void) => void` |
| reduce | `<R>(reducer: (acc: R, item: OinUnit<T>, index: number) => R, initialValue: R) => R` |
| snapshot | `() => T[]` |
| subscribe | `(fn: (v: T[]) => void) => OinUnsubscribe` |
| subscribeUpdate | `(fn: (u: OinUpdate) => void) => OinUnsubscribe` |
| __@iterator@349 | `() => Iterator<OinUnit<T>>` |

## Errors

- This library does not define standardized error codes.
- Mutations can report failures through `onError(listener)` (operation, path, value).

## Source

- `packages/oin/src/lib/utils/types.ts`
