---
title: "OinTreeNode"
description: "type export"
---


## Signature

```ts
OinTreeNode<T, MaxDepth>
```

## Properties

| Name | Type |
| --- | --- |
| snapshot | `(() => unknown[]) \| (() => T) \| (() => T)` |
| subscribe | `((fn: (v: unknown[]) => void) => OinUnsubscribe) \| ((fn: (v: T) => void) => OinUnsubscribe) \| ((fn: (v: T) => void) => OinUnsubscribe)` |
| subscribeUpdate | `((fn: (u: OinUpdate) => void) => OinUnsubscribe) \| ((fn: (u: OinUpdate) => void) => OinUnsubscribe) \| ((fn: (u: OinUpdate) => void) => OinUnsubscribe)` |

## Errors

- This library does not define standardized error codes.
- Mutations can report failures through `onError(listener)` (operation, path, value).

## Source

- `packages/oin/src/lib/utils/types.ts`
