---
title: "formula"
description: "function export"
---


## Signature

```ts
<const D extends readonly FormulaDep[], T>(deps: D, compute: (...args: { [K in keyof D]: DepArg<D[K]>; }) => T) => OinDerived<T>
```

## Parameters

| Name | Type |
| --- | --- |
| deps | `D` |
| compute | `(...args: { [K in keyof D]: DepArg<D[K]>; }) => T` |

## Returns

`OinDerived<T>`

## Errors

- This library does not define standardized error codes.
- Mutations can report failures through `onError(listener)` (operation, path, value).

## Authentication

- Not applicable. OIN is a local TypeScript/JavaScript library and does not implement authentication.

## Source

- `packages/oin/src/lib/formula.ts`
