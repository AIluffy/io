---
title: Lynx
description: 使用 useIO Hook 接入 Lynx（ReactLynx）。
sidebar:
  order: 3
---

npm 包 `@iostore/lynx` 提供 `useIO` 与 `useIOSelector`，用于在 ReactLynx 中订阅 IO 数据源。

## 基础用法

```tsx
import { root } from '@lynx-js/react';
import { io } from '@iostore/store';
import { useIO } from '@iostore/lynx';

const count = io(0);

function Counter() {
  const value = useIO(count);
  return (
    <view>
      <text>{value}</text>
      <view bindtap={() => count.set((v) => v + 1)}>
        <text>+1</text>
      </view>
    </view>
  );
}

root.render(<Counter />);
```

## TypeScript 配置

```json
{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@lynx-js/react"
  }
}
```

## useIOSelector

```tsx
import { useIOSelector } from '@iostore/lynx';

const store = io({ count: 0, other: 0 });

function CountOnly() {
  const count = useIOSelector(store, (state) => state.count);
  return <text>{count}</text>;
}
```

如需自定义比较逻辑：

```tsx
const parity = useIOSelector(store, (state) => ({ p: state.count % 2 }), {
  isEqual: (prev, next) => prev.p === next.p,
});
```

## 调度策略

```tsx
const value = useIO(count, { schedule: 'microtask' });
```

可选值：`sync` | `microtask` | `animationFrame`。

## Query / Resource

安装 Query 包：

```bash
npm i @iostore/query
```

`@iostore/lynx` 提供 `useQuery` 与 `useResource`，并基于 `@iostore/query`：

```tsx
import { useQuery } from '@iostore/lynx';

function UserName(props: { id: number }) {
  const user = useQuery({
    key: ['user', props.id],
    queryFn: async () => {
      'background only';
      return fetch(`/api/users/${props.id}`).then((r) => r.json());
    },
  });
  return <text>{user.data?.name ?? 'loading'}</text>;
}
```

注意：Lynx 的副作用与网络请求应在 background thread 执行，建议在 `queryFn` 中显式标注 `'background only'`。

## 注意事项

- 使用 Lynx 原生标签和事件：`view` / `text` / `input` 与 `bindtap` / `bindinput`。
- 应用入口使用 `root.render(...)`，而不是 Web 的 `createRoot`。

## 下一步

- 结合 [Persist](/behaviors/persist/) 做持久化。
- 阅读 [Lynx 快速开始](https://lynxjs.org/guide/start/quick-start.html)。
