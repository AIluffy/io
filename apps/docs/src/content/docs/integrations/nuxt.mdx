---
title: Nuxt
description: 在 Nuxt 3 中实践 SSR + Hydration。
sidebar:
  order: 8
---

Nuxt SSR 的关键也是同一套原则：

- 每请求创建 store，不做服务端单例。
- hydrate 边界只传 plain 数据。
- server/client 通过 payload 传值，再在客户端重建 store。

## 每请求 Store

把 store 创建放在页面或布局的 `setup` 链路里：

```vue
<!-- pages/dashboard.vue -->
<script setup lang="ts">
import { provide } from 'vue';
import { io } from '@iostore/store';

type AppState = {
  count: number;
  profile: { id: string; name: string } | null;
};

const { data } = await useAsyncData('dashboard', () =>
  $fetch<{ profile: AppState['profile'] }>('/api/dashboard')
);

const initialState: AppState = {
  count: 0,
  profile: data.value?.profile ?? null,
};

const store = io(initialState);
provide('io-store', store);
</script>
```

这会在服务端按请求执行，在客户端按应用实例执行，不会跨请求复用同一份 store。

## Hydrate 边界

Nuxt 的 hydrate 边界通常是 `useAsyncData` / `payload` 注入数据到客户端。

规则：

- 只让边界承载可序列化 JSON。
- store 实例只在运行时创建，不放进 payload。

## Server / Client 传值

推荐流向：`useAsyncData -> initialState(plain) -> io(initialState)`。

```vue
<!-- components/dashboard-panel.vue -->
<script setup lang="ts">
import { inject } from 'vue';
import { ioRef } from '@iostore/vue';
import type { IoScope } from '@iostore/store';

type AppState = {
  count: number;
  profile: { id: string; name: string } | null;
};

const store = inject<IoScope<AppState>>('io-store');
if (!store) throw new Error('io-store is not provided');

const count = ioRef(store.count);
</script>
```

不要做：

- 在模块顶层 `const store = io(...)`。
- 通过 `useState` 传递 store 实例本体。

## 下一步

- 查看 [Vue](/integrations/vue/) 的 `useIO` / `ioRef` 细节。
- 结合 [Schedule](/behaviors/schedule/) 控制通知时机。
