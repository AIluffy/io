---
title: SvelteKit
description: 在 SvelteKit 中实践 SSR + Hydration。
sidebar:
  order: 9
---

SvelteKit 里建议把 SSR 数据流拆成两段：

- 服务端 `load` 只产出可序列化 `initialState`
- 客户端（或同构组件）基于 `initialState` 创建 store

## 每请求 Store

在 `+layout.server.ts`（或 `+page.server.ts`）里按请求准备数据：

```ts
// src/routes/+layout.server.ts
import { io } from '@iostore/store';
import type { LayoutServerLoad } from './$types';

type AppState = {
  count: number;
  user: { id: string; name: string } | null;
};

export const load: LayoutServerLoad = async ({ fetch }) => {
  const user = await fetch('/api/me').then((r) => r.json());
  const store = io<AppState>({ count: 0, user });
  return {
    initialState: store.snapshot(),
  };
};
```

重点：server 端 store 只用于当前请求内计算，不跨请求共享。

## Hydrate 边界

`load` 返回值就是 hydrate 边界。只返回 plain object：

```ts
return { initialState: store.snapshot() };
```

不要把 store 实例直接放进 `load` 返回值。

## Server / Client 传值

在布局组件里接收 `data.initialState` 并创建运行时 store，再通过 `setContext` 下发：

```svelte
<!-- src/routes/+layout.svelte -->
<script lang="ts">
  import { io } from '@iostore/store';
  import { setContext } from 'svelte';
  import type { LayoutData } from './$types';

  type AppState = {
    count: number;
    user: { id: string; name: string } | null;
  };

  export let data: LayoutData;
  const store = io<AppState>(data.initialState);
  setContext('io-store', store);
</script>

<slot />
```

```svelte
<!-- src/routes/+page.svelte -->
<script lang="ts">
  import { getContext } from 'svelte';
  import { toWritable } from '@iostore/svelte';
  import type { IoScope } from '@iostore/store';

  type AppState = {
    count: number;
    user: { id: string; name: string } | null;
  };

  const store = getContext<IoScope<AppState>>('io-store');
  const count = toWritable(store.count);
</script>

<button on:click={() => $count += 1}>{$count}</button>
```

## 下一步

- 查看 [Svelte](/integrations/svelte/) 的 `toReadable` / `toWritable` 细节。
- 结合 [更新日志](/core-concepts/update-log/) 做回放与调试。
