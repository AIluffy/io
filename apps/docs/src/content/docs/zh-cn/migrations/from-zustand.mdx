---
title: 从 Zustand 迁移
description: 常见模式的对应关系。
sidebar:
  order: 1
---

本指南将常见 Zustand 模式映射到 IO。目标是语义对齐，而不是完全一一对应。

## 创建 store

Zustand：

```ts
const useStore = create((set) => ({
  count: 0,
  inc: () => set((s) => ({ count: s.count + 1 })),
}));
```

IO：

```ts
import { io } from 'io-store';

const store = io({ count: 0 });
const inc = () => store.count((v) => v + 1);
```

如果你更偏好“只保留第一层”的 store，可以使用 `shallow: true`。浅层模式
深度限制为 1，并且 `commit` 不允许新增未知 key。

## 选择器

Zustand 选择器：

```ts
const count = useStore((s) => s.count);
```

IO 选择器（derived）：

```ts
import { derived } from 'io-store';

const count = derived(store, (s) => s.count);
```

需要路径聚焦时可使用 `lens`：

```ts
import { lens } from 'io-store';

const userName = lens<string>(store, ['user', 'name']);
```

## 中间件 → 行为扩展

```ts
import { persist, devtools, withBehaviors } from 'io-store/behavior';
import { createIoDevtools } from 'io-devtools';

const view = withBehaviors(store, [
  persist({ key: 'store' }),
  devtools({ target: store, create: createIoDevtools }),
]);
```

## React 使用方式

```tsx
import { useIo } from 'io-react';

function Counter() {
  const value = useIo(store.count);
  return <button onClick={inc}>{value}</button>;
}
```

## 下一步

- 了解 [介绍](/zh-cn/getting-started/introduction/)。
- 查看 [行为扩展](/zh-cn/behaviors/overview/)。
