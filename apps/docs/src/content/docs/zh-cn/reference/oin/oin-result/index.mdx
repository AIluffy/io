---
title: "OinResult"
description: "type export"
---


## 签名

```ts
OinResult<T, MaxDepth>
```

## 属性

| Name | Type |
| --- | --- |
| snapshot | `(() => T) \| (() => unknown[]) \| (() => T)` |
| subscribe | `((fn: (v: T) => void) => OinUnsubscribe) \| ((fn: (v: unknown[]) => void) => OinUnsubscribe) \| ((fn: (v: T) => void) => OinUnsubscribe)` |
| subscribeUpdate | `((fn: (u: OinUpdate) => void) => OinUnsubscribe) \| ((fn: (u: OinUpdate) => void) => OinUnsubscribe) \| ((fn: (u: OinUpdate) => void) => OinUnsubscribe)` |

## 错误

- 本库未定义标准化错误码。
- 变更失败可通过 `onError(listener)` 上报（operation、path、value）。

## 源码

- `packages/oin/src/lib/utils/types.ts`
