---
title: 更新日志
description: 每次写入都会生成结构化 patch。
sidebar:
  order: 4
---

## Patch 与 Update

IO 会将每次写入记录为 `patch`（`set` / `splice` / `sort`），并聚合为 `update`。`update` 可合并、可回放，也可通过反向 `update` 实现撤销。

```ts
import { io } from 'io-store';

const count = io(0);

const unsub = count.subscribeUpdate((update) => {
  console.log(update.patches);
});

count.set(1);
unsub();
```

:::tip
在树的根节点订阅 `subscribeUpdate`，可以捕获整个 store 的变更。
:::

:::note
`subscribeUpdate()` 关注的是“发生了什么变化”（patch）。如果你还需要“最新值”，请用 `snapshot()`/`get()` 读取（或者用 `subscribe()` 直接订阅值）。
:::

## onMutation 与 onError

用调试辅助函数观察变更与错误。

```ts
import { onMutation, onError, io } from 'io-store';

const store = io({ count: 0 });

const stop = onMutation(store, (patch, path) => {
  console.log(path, patch.op, patch.next);
});

const stopError = onError(store, (error, path, op) => {
  console.error('failed', op, path, error);
});
```

## Update 工具函数

```ts
import { applyUpdate, mergeUpdates, undoUpdate, io } from 'io-store';

const store = io({ count: 0 });
const updates = [];
const unsub = store.subscribeUpdate((u) => updates.push(u));

store.count.set(1);
store.count.set(2);
unsub();

const replayStore = io({ count: 0 });
applyUpdate(replayStore, updates);

const merged = mergeUpdates(...updates);
const inverted = undoUpdate(merged);

applyUpdate(store, inverted);
```

## Undo / redo

可直接使用 `createHistory` 快速完成 undo/redo：

```ts
import { createHistory, io } from 'io-store';

const store = io({ count: 0 });
const history = createHistory(store);

store.count.set(1);
store.count.set(2);

history.undo();
history.redo();
```

## 下一步

- 了解 [DevTools](/zh-cn/behaviors/devtools/)。
- 学习 [批处理](/zh-cn/guides/batching/)。
