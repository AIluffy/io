---
title: 深层 Tree
description: 任意深度的路径都是 Unit。
sidebar:
  order: 2
---

## 默认深层

`io()` 会递归处理普通对象与数组，把深层路径变成可读写的节点。你可以直接改到任意深度，而不需要 reducer 或样板代码。

```ts
import { io } from 'io-store';

const state = io({ user: { profile: { name: 'Ada' } } });

state.user.profile.name.get();
state.user.profile.name.set('Grace');
```

## 浅层模式

仅将第一层转为 Unit（深度限制为 1），更深层的值按普通数据对待：

```ts
const state = io({ user: { name: 'Ada' } }, { shallow: true });

state.user.get(); // 返回普通对象
state.user.set({ name: 'Grace' });
```

浅层模式下的 `commit` 不允许新增未知 key，以保持结构稳定。

## 聚焦某条路径

使用 `relocate` 为某条路径创建一个“视图”。适合把局部状态传给函数或组件，而不用暴露整个树。

```ts
import { relocate, io } from 'io-store';

const store = io({ settings: { theme: 'light' } });
const theme = relocate<string>(store, ['settings', 'theme']);

theme.get(); // 'light'
theme.set?.('dark'); // 只有目标是 Unit 时才可写
```

## 下一步

- 查看 [更新日志](/zh-cn/core-concepts/update-log/)。
- 学习 [Derived](/zh-cn/guides/derived/)。
