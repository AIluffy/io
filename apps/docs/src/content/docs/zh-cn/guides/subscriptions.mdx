---
title: 订阅
description: 监听值变化或结构化更新。
sidebar:
  order: 3
---

import SubscriptionsRecipe from '../../../../components/snippets/guides/SubscriptionsRecipe.mdx';

## 30 秒配方

<SubscriptionsRecipe />

## 值订阅

```ts
import { io } from 'io-store';

const count = io(0);

const unsub = count.subscribe((value) => {
  console.log('value', value);
});

count.set(1);
unsub();
```

值订阅适合 UI 渲染与任何需要“最新值”的场景。

## 更新订阅

`subscribeUpdate` 提供结构化 patch 信息。

```ts
const unsubUpdate = count.subscribeUpdate((update) => {
  for (const patch of update.patches) {
    console.log(patch.op, patch.path, patch.next);
  }
});
```

更新订阅适合日志、持久化、同步、DevTools 等需要 patch 的场景。

## Scope / Tree 冒泡

订阅会向上冒泡，在根节点订阅即可捕获任意深层变化。

```ts
const store = io({ user: { name: 'Ada' } });

store.subscribe(() => {
  console.log('root value changed', store.snapshot());
});

store.subscribeUpdate((u) => {
  console.log('root patches', u.patches);
});
```

## 清理订阅

记得调用取消订阅函数（React effect cleanup、Vue scope dispose、Svelte onDestroy 等）。

## 下一步

- 查看 [更新日志](/zh-cn/core-concepts/update-log/)。
- 使用 [DevTools](/zh-cn/behaviors/devtools/)。
