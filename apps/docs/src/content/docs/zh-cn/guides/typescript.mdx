---
title: TypeScript
description: 路径与节点的强类型支持。
sidebar:
  order: 5
---

## Unit 类型

```ts
import type { IoUnit } from 'io-store';
import { io } from 'io-store';

const count: IoUnit<number> = io(0);
```

## 树形类型

`io()` 会保留嵌套结构类型。

```ts
const store = io({ user: { name: 'Ada', age: 36 } });
store.user.name();
```

## Path 工具类型

```ts
import type { IoPathOf, IoPathValue } from 'io-store';

type Store = { user: { profile: { name: string } } };

type NamePath = IoPathOf<Store>;

type NameValue = IoPathValue<Store, ['user', 'profile', 'name']>;
```

## Unwrap 工具类型

```ts
import type { UnwrapIo } from 'io-store';

type Shape = UnwrapIo<ReturnType<typeof io>>;
```

## 下一步

- 阅读 [行为扩展](/zh-cn/behaviors/overview/)。
- 查看 [集成](/zh-cn/integrations/react/)。
