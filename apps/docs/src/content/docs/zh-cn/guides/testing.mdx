---
title: 测试
description: 用快照与 patch 进行测试。
sidebar:
  order: 4
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import TestingRecipe from '../../../../components/snippets/guides/TestingRecipe.mdx';

## 30 秒配方

<TestingRecipe />

## 使用 snapshot 断言

```ts
import { expect, it } from 'vitest';
import { io } from 'io-store';

it('updates count', () => {
  const count = io(0);
  count.update((v) => v + 1);
  expect(count.snapshot()).toBe(1);
});
```

## 测试一个 store 结构

```ts
import { expect, it } from 'vitest';
import { io } from 'io-store';

it('updates deep fields', () => {
  const store = io({ user: { name: 'Ada' } });
  store.user.name.set('Grace');
  expect(store.snapshot()).toEqual({ user: { name: 'Grace' } });
});
```

## 断言 patch

```ts
it('emits update patches', () => {
  const count = io(0);
  const patches: string[] = [];
  const unsub = count.subscribeUpdate((u) => {
    patches.push(u.patches[0].op);
  });

  count.set(1);
  unsub();

  expect(patches).toEqual(['set']);
});
```

<Tabs>
  <TabItem label="Arrange">
    把写入逻辑封装成函数，测试和业务共用。
  </TabItem>
  <TabItem label="Act">
    执行函数并触发更新。
  </TabItem>
  <TabItem label="Assert">
    使用 snapshot 或 patch 断言结果。
  </TabItem>
</Tabs>

## 小建议

- 把写入操作封装成普通函数，业务和测试复用。
- 行为正确性优先用快照断言；需要审计/回放时再断言 patch。

## 下一步

- 阅读 [TypeScript](/zh-cn/guides/typescript/)。
- 体验 [Playground](/zh-cn/guides/playground/)。
