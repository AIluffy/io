---
title: Vue
description: 使用 useIO 与 ioRef 接入 Vue。
sidebar:
  order: 3
---

`io-vue` 提供 `useIO`（只读）与 `ioRef`（双向绑定）。

## useIO

```ts
import { io } from 'io-store';
import { useIO } from 'io-vue';

const store = io({ count: 0 });

const state = useIO(store.count);
```

`useIO` 返回 `ShallowRef`，适合在模板中直接使用。

## ioRef（双向绑定）

```ts
import { ioRef } from 'io-vue';

const countRef = ioRef(store.count);
```

## 调度策略

```ts
const state = useIO(store.count, { schedule: 'microtask' });
```

可选值：`sync` | `microtask` | `animationFrame`。

## 下一步

- 查看 [派生值](/zh-cn/guides/derived/) 的组合方式。
- 了解 [Schedule](/zh-cn/behaviors/schedule/) 合并通知。
