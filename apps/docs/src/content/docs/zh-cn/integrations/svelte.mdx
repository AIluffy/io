---
title: Svelte
description: 将 IO Unit 适配为 Svelte store。
sidebar:
  order: 4
---

## toReadable

```ts
import { io } from 'io-store';
import { toReadable } from 'io-svelte';

const count = io(0);
export const countReadable = toReadable(count);
```

## toWritable

```ts
import { toWritable } from 'io-svelte';

export const countWritable = toWritable(count);
```

## 下一步

- 查看 [React 集成](/zh-cn/integrations/react/)。
- 阅读 [行为扩展概览](/zh-cn/behaviors/overview/)。
