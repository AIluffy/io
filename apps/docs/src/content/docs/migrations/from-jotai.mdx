---
title: 从 Jotai 迁移
description: 将 atom 模式映射到 IO。
sidebar:
  order: 2
---

本指南将常见 Jotai 用法映射到 IO，并保持更新语义一致。

## 基础 atom

Jotai：

```ts
import { atom } from 'jotai';

export const countAtom = atom(0);
```

IO：

```ts
import { io } from '@iostore/store';

export const count = io(0);
```

## 派生值

Jotai：

```ts
const doubleAtom = atom((get) => get(countAtom) * 2);
```

IO：

```ts
import { derived } from '@iostore/store';

const double = derived([count], (v) => v * 2);
```

## 可写派生逻辑

Jotai：

```ts
const incAtom = atom(null, (_get, set) => {
  set(countAtom, (v) => v + 1);
});
```

IO：

```ts
const inc = () => count.set((v) => v + 1);
```

## React 接入

Jotai：

```tsx
const [count, setCount] = useAtom(countAtom);
```

IO：

```tsx
import { useIO } from '@iostore/react';

function Counter() {
  const value = useIO(count);
  return <button onClick={() => count.set((v) => v + 1)}>{value}</button>;
}
```

## 下一步

- 阅读 [介绍](/getting-started/introduction/)。
- 查看 [Derived 指南](/guides/derived/)。
