---
title: 从 Redux 迁移
description: 将 reducer/action 模式迁移到 IO。
sidebar:
  order: 4
---

本指南展示 Redux 风格更新如何映射到 IO 的基础能力。

## Store + Reducer

Redux：

```ts
type State = { count: number };
type Action = { type: 'inc' } | { type: 'set'; payload: number };

function reducer(state: State = { count: 0 }, action: Action): State {
  switch (action.type) {
    case 'inc':
      return { ...state, count: state.count + 1 };
    case 'set':
      return { ...state, count: action.payload };
    default:
      return state;
  }
}
```

IO：

```ts
import { io } from 'io-store';

const store = io({ count: 0 });

const inc = () => store.count.set((v) => v + 1);
const setCount = (value: number) => store.count.set(value);
```

## 多字段更新

Redux 通常在 reducer 中协调；在 IO 中可使用 `commit`：

```ts
const user = io({ name: 'Ada', age: 18 });

const updateProfile = (name: string, age: number) => {
  user.commit((draft) => {
    draft.name = name;
    draft.age = age;
  });
};
```

## 类似时间旅行的撤销/重做

```ts
import { createHistory } from 'io-store';

const history = createHistory(store);
history.undo();
history.redo();
```

## React 接入

```tsx
import { useIO } from 'io-react';

function Counter() {
  const value = useIO(store.count);
  return <button onClick={inc}>{value}</button>;
}
```

## 下一步

- 阅读 [更新日志](/core-concepts/update-log/)。
- 查看 [行为扩展](/behaviors/overview/)。
