---
import PiniaCounter from './pinia/PiniaCounter.vue';
---

<section>
  <h4>Pinia + Vue（client:load）</h4>
  <PiniaCounter client:load />

  <h4>Pinia + Vue（client:only="vue"）</h4>
  <!-- client:only="vue" 会在客户端创建 Vue 应用并挂载。 -->
  <PiniaCounter client:only="vue" />
</section>
