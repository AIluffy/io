---
import ZustandCounter from './zustand/ZustandCounter.tsx';
---

<section>
  <h4>Zustand + React（client:load）</h4>
  <ZustandCounter client:load />

  <h4>Zustand + React（client:only="react"）</h4>
  <!-- client:only 适合“仅客户端可用”的依赖或逻辑。 -->
  <ZustandCounter client:only="react" />
</section>
