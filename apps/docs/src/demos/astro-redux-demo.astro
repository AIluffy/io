---
import ReduxCounter from './redux/ReduxCounter.tsx';
---

<section>
  <h4>Redux + React（client:load）</h4>
  <ReduxCounter client:load />

  <h4>Redux + React（client:only="react"）</h4>
  <!-- client:only 不参与 SSR，不会输出初始 HTML。 -->
  <ReduxCounter client:only="react" />
</section>
